now as of now if we start the container (docker compose up) then be default docker compose allows
only one user to create for given database

as of now we have 3 MS and all 3 have different databases and we wil have 3 users (3 spring boot apps) who
will access those db so
after doing docker compose up and before starting all 3 MS we manually need to create 3 DB and 3 users
with given username and password, failing to do so will cause error in starting the spring boot app as
postgres server running at jdbc:postgresql://localhost:5432 do not have those 3 DBs

we can automate things when docker container starts but it's ok to create those 3 DBs manually....