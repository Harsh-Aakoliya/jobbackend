spring.application.name=companyms
server.port=8081

# H2 related configuration
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console
#
#spring.datasource.url=jdbc:h2:mem:testdb
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=
#
#spring.jpa.show-sql=true
#spring.jpa.hibernate.ddl-auto=create-drop



# postgres related configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/company
# we can create a separate user called company but we need to give it canLogin access and all..
#so for simplicity we can use a newly created user (using that docker compose..) for all 3 MS
spring.datasource.username = jobbackend
spring.datasource.password = jobbackend

spring.jpa.database = POSTGRESQL
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.database-platform = org.hibernate.dialect.PostgreSQLDialect

#Eureka client
eureka.client.register-with-eureka = true
eureka.client.fetch-registry = true
#just define where the eureka server is running
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/

#zipkin
management.tracing.sampling.probability=1.0